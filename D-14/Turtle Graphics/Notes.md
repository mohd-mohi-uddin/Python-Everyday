to import all the files from the module we can easily type from module name import "*".

this will import all the classes, methods.

>> import "module name" as "name"
if the module name is way big u can rename this and use it in your code. 
eg:

import turtle as t