>> CLASSES AND OBJECTS:

We can create class using the class ClassName:

in the class we write functions which are called as methods
but here the fuctions are dicvided into two parts that are attributes and methods.

the attributes are written in constructors which is init.
where we declare variable name using self argument 

and behinf it we can writwe our code 

we can writw our another function in there

now to use this make an onject:

the oject is created just by giving class as a data to a varible
eg: objectname = ClassName

now what ever attributes u want to use can be obtained using the objectname.variablename which were declared in the constructor.

