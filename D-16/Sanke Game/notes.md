>>> INHERITANCE IN OOPS:

in oop we can copy the behaviour of one class to a new class using syntax

    class newclass(packagename,parentclass)

    and in the constructor pass super().__init__()

>>there is in built function called "abs()" which gives uu the excat value of the integer 

like if there is -270 it will give 270.

>> use abs() if u have to use two negitive and positive same number to code.
eg : if < 270 and > -270. Use if abs(270)


>>> THERE IS ONE MORE RANDOM FUNCTION:

There is randrange which is similar to randint

both take int values which are inclusive

but the range range give a extra parameter called step

randint has (start,stop)
randrange has (start, stop , step)

>>> USING TIME MODULE:

we can give gap in between iterations using time.sleep(0.1) 

this will delsy every iterstion by 0.1 sec.

but first u need to import time module 

>>> SLICING INSTEAD OF RANGE:

use slicing in for loop instead of range for i in list[start:stop:step]

