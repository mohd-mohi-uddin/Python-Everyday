1. TO USE BEAUTIFUL SOAP:

use classname(contents u copied using file.read(), the parser-html.parser)

2. prettify()

we can use ,prettify tothe obect to make the structure good.

3. object.find_all(name="")

this is used to find all the tags with the name: h1 or a or p in the list

4. tag.getText():

to get the text of that tag

5. object.find(name="a", id="" or class_ = ""or any other attribute)

we can get a particular tag

6. tag.get("href" or any attribute):

to get the particular attriobute value.

7. tag.select_one(selector = "tag,class,id")

to select one from the body

8. tag.select(selector = "tag,class,id")
 to get all from the body

9. css selector object.select(parent tag then child tag then its child tag)


