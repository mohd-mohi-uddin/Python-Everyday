>> CREATING INVIRONMENT VARIABLES:

to create the evironment virable first we need tto create a .env file to sotore variables

then in main file import os then from dotenv import load_dotenv

now call load_dotenv()

now use os.getenv(""variables in .env) as the values to new variables in main.py

